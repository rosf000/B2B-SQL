# 02 函式、模組與例外處理除錯實務

> **📌 本章定位**：新手工程師追求「程式能跑就好」，資深資料工程師追求「**即使炸掉也能清楚知道死在哪裡、如何優雅恢復**」。本篇的核心心智模型是：將混亂的腳本拆解為具備型別註解的純函式（Pure Functions），並以生產級 Logging 與 Exception Hierarchy 建立管線的「黑盒子飛航記錄器」。
>
> **⚠️ 痛點場景（生產環境三大鬼故事）**：
> 1. **吞掉例外的無聲崩潰**：寫了 `except Exception: pass`，資料庫密碼過期連不上，程式不報錯卻默默把空資料覆蓋進線上表，三天後老闆發現營收報表全部歸零。
> 2. **可變預設引數（Mutable Default Argument）幽靈 Bug**：定義 `def add_item(item, items=[])`，以為每次都是新清單，結果第 2 次呼叫繼承了第 1 次的歷史資料，跨客戶數據嚴重混雜。
> 3. **隨手 print() 的排查地獄**：腳本在半夜 Crontab 自動執行失敗，因為只有 `print("error")`，既沒有時間戳、沒有錯誤堆疊（Traceback），也沒有寫入日誌檔，根本無從查起。
>
> **💡 學習策略**：先定位（模組化與日誌階層）➔ 再理解（例外捕捉原則與裝飾器機制）➔ 再操作（動手寫防禦性工具函式）➔ 再回收（生產級除錯清單）。

---

## 🗺️ 本篇學習地圖

```
函式（Function）
  ├── 基本定義與型別提示
  ├── 預設參數 / *args / **kwargs
  ├── 純函式 vs 有副作用的函式
  └── Decorator（裝飾器）

模組與套件
  ├── import 的各種方式
  ├── __name__ == "__main__"
  └── 建立自己的模組

例外處理
  ├── try / except / else / finally
  ├── 自定義例外類別
  └── 良好的錯誤訊息設計

除錯實務
  ├── print → logging 的升級路
  ├── logging 模組完整設定
  └── pdb 與 VS Code 除錯器
```

---

## 一、函式（Function）

### 1.1 基本定義與型別提示

```python
# 基本函式定義
def greet(name: str) -> str:
    """
    向指定的業務員打招呼。

    Args:
        name: 業務員姓名

    Returns:
        問候字串
    """
    return f"早安，{name}！"

# 型別提示（Type Hints）不是強制的，但能大幅提升可讀性和 IDE 支援
def calculate_achievement(
    actual_revenue: float,
    target: float
) -> dict:
    """計算業績達成率，並回傳分析結果。"""
    if target <= 0:
        raise ValueError(f"目標業績必須大於 0，收到：{target}")

    rate = actual_revenue / target * 100
    return {
        "actual": actual_revenue,
        "target": target,
        "rate_pct": round(rate, 1),
        "status": "達標" if rate >= 100 else "未達標"
    }

result = calculate_achievement(1_200_000, 1_000_000)
print(result)  # {'actual': 1200000, 'target': 1000000, 'rate_pct': 120.0, 'status': '達標'}
```

**型別提示的完整語法**：

```python
from typing import Optional, Union, List, Dict, Tuple

def process_orders(
    order_ids: List[int],                          # List[元素型別]
    customer_map: Dict[int, str],                  # Dict[key型別, value型別]
    status: Optional[str] = None,                  # Optional = 可以是 None
    threshold: Union[int, float] = 100_000         # Union = 可以是多種型別
) -> Tuple[List[int], int]:                        # 回傳 Tuple
    """處理指定的訂單列表。"""
    processed = [oid for oid in order_ids if oid in customer_map]
    return processed, len(processed)
```

---

### 1.2 預設參數 / *args / **kwargs

```python
# 預設參數（必填參數放前面，選填放後面）
def send_alert(
    message: str,
    recipient: str,
    channel: str = "email",     # 預設發 email
    priority: int = 2           # 預設優先度 2
) -> None:
    print(f"[{priority}] 發送到 {recipient} via {channel}：{message}")

send_alert("業績達標！", "wang@company.com")                   # 使用預設值
send_alert("庫存不足", "chen@company.com", "slack", 1)         # 覆蓋預設值
send_alert("系統告警", "admin@company.com", priority=1)        # 關鍵字引數

# ⚠️ 陷阱：不要用可變物件（list/dict）當預設值
def bad_func(data=[]):      # ❌ 所有呼叫共享同一個 list！
    data.append(1)
    return data

def good_func(data=None):   # ✅ 正確做法
    if data is None:
        data = []
    data.append(1)
    return data
```

```python
# *args：接收任意數量的位置引數（打包為 tuple）
def sum_revenues(*amounts: float) -> float:
    """計算任意數量業績的總和。"""
    return sum(amounts)

sum_revenues(100_000, 200_000, 300_000)  # 三個引數
sum_revenues(500_000)                    # 一個引數

# **kwargs：接收任意數量的關鍵字引數（打包為 dict）
def create_customer(**kwargs: str) -> dict:
    """用關鍵字引數建立客戶資料。"""
    required = {"company_name", "industry", "city"}
    missing = required - set(kwargs.keys())
    if missing:
        raise ValueError(f"缺少必要欄位：{missing}")
    return kwargs

customer = create_customer(
    company_name="台灣半導體",
    industry="Semiconductor",
    city="台北市",
    credit_limit="5000000"    # 額外的欄位也能傳入
)

# 組合使用（順序固定：一般參數 → *args → 關鍵字參數 → **kwargs）
def build_report(title: str, *sections: str, author: str = "系統", **metadata):
    print(f"報告：{title}，作者：{author}")
    for section in sections:
        print(f"  章節：{section}")
    for k, v in metadata.items():
        print(f"  {k}: {v}")

build_report(
    "月度業績報告",
    "業績摘要", "客戶分析", "產品分析",  # *sections
    author="王小明",                       # 關鍵字參數
    period="2024-09",                     # **metadata
    format="PDF"
)
```

---

### 1.3 Decorator（裝飾器）

裝飾器是 Python 最優雅的特性之一，讓你在不修改函式本身的情況下，添加額外的功能（如計時、重試、驗證）。

**基本概念**：裝飾器是一個接收函式的函式，回傳一個新的函式。

```python
import time
import functools

# 裝飾器範例 1：計時器（測量函式執行時間）
def timer(func):
    @functools.wraps(func)   # 保留原函式的 __name__ 和 __doc__
    def wrapper(*args, **kwargs):
        start = time.perf_counter()
        result = func(*args, **kwargs)       # 執行原函式
        elapsed = time.perf_counter() - start
        print(f"[TIMER] {func.__name__} 執行時間：{elapsed:.3f}s")
        return result
    return wrapper

@timer
def slow_query():
    """模擬一個慢查詢。"""
    time.sleep(1.5)
    return "查詢完成"

slow_query()  # 自動列印執行時間
# [TIMER] slow_query 執行時間：1.502s
```

```python
# 裝飾器範例 2：重試機制（網路請求或資料庫連線失敗時自動重試）
import time

def retry(max_attempts: int = 3, delay: float = 1.0, exceptions=(Exception,)):
    """
    自動重試裝飾器。

    Args:
        max_attempts: 最大重試次數
        delay: 每次重試前等待的秒數
        exceptions: 要捕捉的例外類型
    """
    def decorator(func):
        @functools.wraps(func)
        def wrapper(*args, **kwargs):
            last_exception = None
            for attempt in range(1, max_attempts + 1):
                try:
                    return func(*args, **kwargs)
                except exceptions as e:
                    last_exception = e
                    if attempt < max_attempts:
                        print(f"[RETRY] {func.__name__} 第 {attempt} 次失敗：{e}，{delay}s 後重試...")
                        time.sleep(delay)
                    else:
                        print(f"[RETRY] {func.__name__} 已達最大重試次數 ({max_attempts})")
            raise last_exception
        return wrapper
    return decorator

@retry(max_attempts=3, delay=2.0, exceptions=(ConnectionError,))
def fetch_from_api(url: str) -> dict:
    """從 API 取得資料（可能因網路問題失敗）。"""
    # 實際程式碼會呼叫 requests.get(url)
    raise ConnectionError("連線逾時")
```

```python
# 裝飾器範例 3：輸入驗證
def validate_positive(func):
    """確保函式的所有數值引數都是正數。"""
    @functools.wraps(func)
    def wrapper(*args, **kwargs):
        for arg in args:
            if isinstance(arg, (int, float)) and arg <= 0:
                raise ValueError(f"{func.__name__} 的引數必須是正數，收到：{arg}")
        return func(*args, **kwargs)
    return wrapper

@validate_positive
def calculate_margin(revenue: float, cost: float) -> float:
    return (revenue - cost) / revenue * 100

calculate_margin(100_000, 60_000)   # ✅ 正常
calculate_margin(-100_000, 60_000)  # ❌ 拋出 ValueError
```

---

## 二、模組與套件

### 2.1 import 的各種方式

```python
# 方式 1：import 整個模組（推薦，最清晰）
import os
import datetime
import collections

path = os.path.join("data", "orders.csv")
today = datetime.date.today()

# 方式 2：from ... import 特定名稱
from datetime import date, timedelta
from collections import defaultdict, Counter

yesterday = date.today() - timedelta(days=1)

# 方式 3：import ... as 別名（常用於長名稱）
import pandas as pd
import sqlalchemy as sa
from sqlalchemy.orm import sessionmaker as SessionMaker

# ❌ 避免：from module import *（污染命名空間，難以追蹤來源）
from os import *   # 不要這樣做！
```

### 2.2 `__name__ == "__main__"` 的用途

```python
# db_utils.py

def connect_to_db(url: str):
    """連線到資料庫。"""
    # ... 實作 ...
    pass

def get_customer(customer_id: int) -> dict:
    """查詢客戶資料。"""
    # ... 實作 ...
    pass

# 這一段只在「直接執行這個檔案」時才會跑
# 若別的檔案 import db_utils，這段不會執行
if __name__ == "__main__":
    # 用於測試這個模組的功能
    print("測試資料庫連線...")
    conn = connect_to_db("postgresql://...")
    customer = get_customer(1)
    print(f"查詢結果：{customer}")
```

---

## 三、例外處理（Exception Handling）

### 3.1 try / except / else / finally 完整語法

```python
def safe_divide(a: float, b: float) -> float:
    """安全的除法，處理除以零的情況。"""
    try:
        result = a / b          # 可能出錯的程式碼
    except ZeroDivisionError:
        print("錯誤：除數不能為零")
        return 0.0
    except TypeError as e:
        print(f"型別錯誤：{e}")
        raise                   # 重新拋出，讓呼叫者處理
    else:
        # 只在 try 成功（沒有例外）時執行
        print(f"計算成功：{a} / {b} = {result}")
        return result
    finally:
        # 無論成功或失敗都會執行（適合釋放資源）
        print("除法操作結束")
```

**常見的例外類型**：

```python
# Python 內建例外繼承樹（常用部分）
Exception
├── ValueError       # 值不合法（如 int("abc")）
├── TypeError        # 型別不合法（如 1 + "a"）
├── KeyError         # Dict 的 key 不存在
├── IndexError       # List 索引超出範圍
├── AttributeError   # 物件沒有這個屬性
├── FileNotFoundError # 檔案不存在
├── IOError          # 輸入輸出錯誤
├── ConnectionError  # 網路連線錯誤
├── TimeoutError     # 連線逾時
└── PermissionError  # 權限不足
```

### 3.2 自定義例外類別

```python
# 為你的應用程式定義語義清晰的例外
class B2BException(Exception):
    """B2B 系統的基礎例外類別。"""
    pass

class CustomerNotFoundError(B2BException):
    """客戶不存在時拋出。"""
    def __init__(self, customer_id: int):
        self.customer_id = customer_id
        super().__init__(f"客戶 ID {customer_id} 不存在")

class InsufficientStockError(B2BException):
    """庫存不足時拋出。"""
    def __init__(self, product_id: int, requested: int, available: int):
        self.product_id = product_id
        self.requested = requested
        self.available = available
        super().__init__(
            f"產品 {product_id} 庫存不足：需要 {requested} 個，現有 {available} 個"
        )

class CreditLimitExceededError(B2BException):
    """超過信用額度時拋出。"""
    def __init__(self, customer_id: int, order_amount: float, credit_limit: float):
        self.customer_id = customer_id
        self.order_amount = order_amount
        self.credit_limit = credit_limit
        super().__init__(
            f"客戶 {customer_id} 信用額度不足：訂單金額 {order_amount:,.0f}，額度 {credit_limit:,.0f}"
        )

# 使用自定義例外
def create_order(customer_id: int, product_id: int, quantity: int):
    # 查詢客戶
    customer = db_get_customer(customer_id)
    if not customer:
        raise CustomerNotFoundError(customer_id)

    # 查詢庫存
    product = db_get_product(product_id)
    if product["stock"] < quantity:
        raise InsufficientStockError(product_id, quantity, product["stock"])

    # 計算金額並確認信用額度
    total = product["price"] * quantity
    if total > customer["credit_limit"]:
        raise CreditLimitExceededError(customer_id, total, customer["credit_limit"])

    # 實際建立訂單...

# 呼叫端的處理
try:
    create_order(customer_id=5, product_id=101, quantity=10)
except CustomerNotFoundError as e:
    print(f"客戶不存在：{e.customer_id}")
except InsufficientStockError as e:
    print(f"庫存不足：需要 {e.requested}，僅有 {e.available}")
except CreditLimitExceededError as e:
    print(f"信用額度超過：金額 {e.order_amount:,.0f}，額度 {e.credit_limit:,.0f}")
except B2BException as e:
    # 捕捉所有 B2B 系統的例外
    print(f"業務錯誤：{e}")
except Exception as e:
    # 未預期的系統錯誤
    print(f"系統錯誤（請聯絡工程師）：{type(e).__name__}: {e}")
    raise  # 重新拋出，讓上層處理或記錄
```

---

## 四、logging 模組 — 取代 print 的最佳實踐

### 4.1 為什麼不要用 print？

| | `print` | `logging` |
|---|---|---|
| 日誌等級 | 無 | DEBUG/INFO/WARNING/ERROR/CRITICAL |
| 時間戳記 | 需手動加 | 自動加 |
| 輸出目標 | 只能螢幕 | 螢幕 + 檔案 + 遠端 |
| 生產環境關閉 | 要手動刪除 | 調整 level 即可 |
| 格式化 | 手動 | 統一設定 |

### 4.2 logging 完整設定

```python
# config/logging_config.py
import logging
import logging.handlers
import os
from datetime import datetime

def setup_logging(
    log_name: str = "b2b_app",
    log_dir: str = "logs",
    level: int = logging.INFO
) -> logging.Logger:
    """
    設定應用程式的日誌系統。

    Returns:
        設定好的 Logger 物件
    """
    # 建立 logs 資料夾
    os.makedirs(log_dir, exist_ok=True)

    # 建立 Logger
    logger = logging.getLogger(log_name)
    logger.setLevel(logging.DEBUG)  # Logger 本身設最低，由 Handler 控制輸出等級

    # 格式化字串
    formatter = logging.Formatter(
        fmt="%(asctime)s [%(levelname)-8s] %(name)s:%(lineno)d - %(message)s",
        datefmt="%Y-%m-%d %H:%M:%S"
    )
    # 輸出範例：
    # 2024-09-15 10:30:45 [INFO    ] b2b_app:42 - 成功建立訂單 ORD-2024-001

    # Handler 1：輸出到終端（DEBUG 以上）
    console_handler = logging.StreamHandler()
    console_handler.setLevel(logging.DEBUG)
    console_handler.setFormatter(formatter)

    # Handler 2：輸出到檔案，自動按日期切割（INFO 以上）
    log_file = os.path.join(log_dir, f"{log_name}.log")
    file_handler = logging.handlers.TimedRotatingFileHandler(
        filename=log_file,
        when="midnight",    # 每天午夜切割
        backupCount=30,     # 保留 30 天的日誌
        encoding="utf-8"
    )
    file_handler.setLevel(logging.INFO)
    file_handler.setFormatter(formatter)

    # Handler 3：只記錄 ERROR 以上的嚴重錯誤（可串接到告警系統）
    error_log_file = os.path.join(log_dir, f"{log_name}_error.log")
    error_handler = logging.FileHandler(error_log_file, encoding="utf-8")
    error_handler.setLevel(logging.ERROR)
    error_handler.setFormatter(formatter)

    # 加入所有 Handler（避免重複加入）
    if not logger.handlers:
        logger.addHandler(console_handler)
        logger.addHandler(file_handler)
        logger.addHandler(error_handler)

    return logger


# 使用範例
logger = setup_logging("etl_pipeline")

def process_orders_etl(date_str: str):
    logger.info(f"開始處理 {date_str} 的訂單 ETL")

    try:
        orders = fetch_orders(date_str)
        logger.debug(f"取得 {len(orders)} 筆訂單")

        for order in orders:
            try:
                process_single_order(order)
                logger.info(f"訂單 {order['order_number']} 處理完成")
            except InsufficientStockError as e:
                logger.warning(f"庫存不足跳過：{e}")
            except Exception as e:
                logger.error(f"訂單 {order['order_number']} 處理失敗：{e}", exc_info=True)
                # exc_info=True 會附上完整的 traceback

    except Exception as e:
        logger.critical(f"ETL 管線崩潰：{e}", exc_info=True)
        raise

    logger.info(f"{date_str} ETL 完成")
```

**五個日誌等級的使用場景**：

```python
logger.debug("資料庫查詢：SELECT * FROM orders WHERE id=%s", order_id)  # 開發除錯用
logger.info("成功處理訂單 ORD-2024-001，金額 598,000")                  # 正常業務流程
logger.warning("客戶信用額度即將到達上限（已使用 85%）")                 # 需要注意但不緊急
logger.error("無法連接資料庫，已重試 3 次", exc_info=True)              # 功能失敗
logger.critical("系統核心模組崩潰，ETL 管線已停止", exc_info=True)       # 系統層級錯誤
```

---

## 五、除錯實務

### 5.1 pdb — Python 內建除錯器

```python
# 方式 1：直接在程式碼中插入中斷點
import pdb

def buggy_function(orders):
    total = 0
    for order in orders:
        pdb.set_trace()    # 程式在此暫停，進入互動式除錯
        total += order["amount"]  # 可以在這裡檢查 order 的內容
    return total

# pdb 常用指令：
# n (next)      → 執行下一行
# s (step)      → 進入函式內部
# c (continue)  → 繼續執行到下一個中斷點
# p <expr>      → 列印運算式的值（如 p order["amount"]）
# pp <expr>     → 漂亮列印（pretty print）
# l (list)      → 顯示當前位置附近的程式碼
# q (quit)      → 退出除錯器

# 方式 2：Python 3.7+ 的 breakpoint()（不需要 import pdb）
def process_data(data):
    result = []
    for item in data:
        breakpoint()    # 等同於 pdb.set_trace()
        processed = transform(item)
        result.append(processed)
    return result
```

### 5.2 VS Code 除錯設定

在 VS Code 中按 F5 或設定 `launch.json`：

```json
// .vscode/launch.json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: 目前檔案",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}",
                "DATABASE_URL": "postgresql://postgres:password@localhost:5432/b2b_db"
            }
        },
        {
            "name": "Python: ETL 管線",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/scripts/etl_pipeline.py",
            "args": ["--date", "2024-09-15"],
            "console": "integratedTerminal"
        }
    ]
}
```

**VS Code 除錯技巧**：
- **F9**：在當前行設定中斷點（紅點）
- **F5**：開始除錯
- **F10**：執行下一行（Step Over）
- **F11**：進入函式（Step Into）
- **Shift+F11**：離開函式（Step Out）
- **監看視窗**：輸入變數名稱即時查看值

---

## 六、實用模式：Context Manager（with 語句）

```python
# 資料庫連線的正確關閉方式
# ❌ 危險寫法：若中途出錯，連線永遠不會關閉！
conn = db.connect()
result = conn.execute("SELECT ...")  # 若這裡出錯...
conn.close()  # ...這行永遠不會執行

# ✅ 正確寫法：with 確保無論如何都會執行 close
with db.connect() as conn:
    result = conn.execute("SELECT ...")
# 離開 with 區塊時，自動呼叫 conn.close()

# 讀寫檔案的標準寫法
with open("orders.csv", "r", encoding="utf-8") as f:
    content = f.read()
# 不需要 f.close()，with 自動處理

# 自定義 Context Manager（用 dataclass 或 contextlib）
from contextlib import contextmanager

@contextmanager
def db_transaction(conn):
    """資料庫交易的 Context Manager。"""
    try:
        yield conn              # 進入 with 區塊
        conn.commit()           # 成功：提交
    except Exception:
        conn.rollback()         # 失敗：回滾
        raise
    finally:
        conn.close()            # 無論如何：關閉連線

# 使用
with db_transaction(get_connection()) as conn:
    conn.execute("UPDATE orders SET status='COMPLETED' WHERE order_id=1")
    conn.execute("UPDATE products SET stock=stock-2 WHERE product_id=101")
# 兩個 UPDATE 都成功 → 自動 commit
# 任一 UPDATE 失敗 → 自動 rollback
```

---

## 七、建立自己的工具模組

```python
# src/utils.py — 專案通用工具函式

import logging
import os
from typing import Any, Optional
from dotenv import load_dotenv

load_dotenv()  # 從 .env 檔案讀取環境變數

logger = logging.getLogger(__name__)


def get_env(key: str, default: Optional[str] = None) -> str:
    """
    安全地取得環境變數。

    Args:
        key: 環境變數名稱
        default: 若不存在時的預設值（若無預設且不存在，拋出例外）

    Raises:
        EnvironmentError: 環境變數不存在且沒有預設值
    """
    value = os.getenv(key, default)
    if value is None:
        raise EnvironmentError(f"必要的環境變數 '{key}' 未設定。請在 .env 檔案中加入此變數。")
    return value


def format_currency(amount: float, symbol: str = "NT$") -> str:
    """將數值格式化為貨幣字串。"""
    return f"{symbol}{amount:,.0f}"


def chunks(lst: list, n: int):
    """
    將清單切分為大小為 n 的批次（Generator）。

    用法：
        for batch in chunks(order_ids, 100):
            process_batch(batch)
    """
    for i in range(0, len(lst), n):
        yield lst[i:i + n]


def safe_get(data: dict, *keys: str, default: Any = None) -> Any:
    """
    安全地從巢狀字典取值，避免 KeyError。

    用法：
        safe_get(order, "customer", "city")  # order["customer"]["city"]
        safe_get(order, "customer", "phone", default="未填寫")
    """
    try:
        for key in keys:
            data = data[key]
        return data
    except (KeyError, TypeError):
        return default


# 使用範例
if __name__ == "__main__":
    db_url = get_env("DATABASE_URL")
    print(format_currency(1_234_567))  # NT$1,234,567

    order_ids = list(range(1, 101))  # 100 筆訂單 ID
    for batch in chunks(order_ids, 10):
        print(f"處理批次：{batch[:3]}...（共 {len(batch)} 筆）")
```

---

## 八、✋ 空白頁挑戰：生產級 Python Code Review 診斷實戰

> 🎯 **挑戰情境**：
> 以下是實習生提交的一段自動化匯入腳本，請在不看解答的情況下，**抓出至少 4 個重大設計地雷**，並動手重構成符合 Production 標準的代碼！

```python
# 實習生寫的危險腳本
import time

def process_user_batch(users, cache=[]):
    cache.extend(users)
    print(f"目前累計處理：{len(cache)} 位使用者")
    return cache

def export_logs(filename, data):
    f = open(filename, "w")
    try:
        f.write(str(data))
    except:
        pass
```

<details>
<summary>🔍 點擊展開「思維引導與地雷診斷」</summary>

1. **地雷 1：可變預設參數（Mutable Default Argument）**
   - `def process_user_batch(users, cache=[])`：在 Python 中，函式的預設引數只會在模組載入（定義）時被評估一次。所有後續呼叫若不傳入 `cache`，會共享同一個記憶體位址中的列表，造成資料跨批次交叉污染！
   - **解法**：應設定預設為 `cache=None`，並在函式內部使用 `if cache is None: cache = []`。
2. **地雷 2：裸露捕捉例外與靜默吞掉（Bare Except + Pass）**
   - `except: pass` 吞掉了所有例外（甚至包括使用者鍵盤中斷 `KeyboardInterrupt` 與記憶體不足 `MemoryError`），磁碟寫入失敗完全不報錯，資料遺失無人知曉！
   - **解法**：指定明確的例外型別（如 `IOError`、`OSError`），使用 `logger.error(..., exc_info=True)` 記錄錯誤，並決定是否 re-raise。
3. **地雷 3：檔案資源洩漏（Resource Leak）**
   - `open()` 沒有使用 Context Manager（`with`），若 `write()` 前拋出例外，檔案控制代碼（File Descriptor）將不會被關閉。
   - **解法**：一律使用 `with open(...) as f:`。
4. **地雷 4：濫用 print 缺乏時序追蹤**
   - 生產環境排程腳本應使用 `logging` 模組，標註時間戳、日誌等級與模組名稱。
</details>

<details>
<summary>🔑 點擊展開「重構後 Production 代碼」</summary>

```python
import logging
from typing import List, Optional

# 配置模組級日誌記錄器
logger = logging.getLogger(__name__)

def process_user_batch(users: List[dict], cache: Optional[List[dict]] = None) -> List[dict]:
    """安全處理批次使用者資料，防範可變預設引數污染。"""
    if cache is None:
        cache = []
    
    cache.extend(users)
    logger.info("本批次處理完成，目前緩存累計: %d 筆", len(cache))
    return cache

def export_logs(filename: str, data: object) -> None:
    """使用 Context Manager 安全寫入檔案，並妥善處理例外。"""
    try:
        with open(filename, "w", encoding="utf-8") as f:
            f.write(str(data))
        logger.info("日誌成功導出至: %s", filename)
    except OSError as e:
        logger.error("無法寫入日誌檔 %s: %s", filename, str(e), exc_info=True)
        raise  # 向上拋出讓調用方知曉失敗
```
</details>

---

## 九、本章重點彙整

```
函式設計
  型別提示：def func(x: int) -> str 提升可讀性
  預設參數：必填在前，選填在後，不要用可變物件當預設值
  *args → 不定數量位置引數（打包為 tuple）
  **kwargs → 不定數量關鍵字引數（打包為 dict）

Decorator 三種應用
  @timer      → 效能監控
  @retry(3)   → 自動重試（含參數的裝飾器）
  @validate   → 輸入驗證

例外處理
  try/except/else/finally 四段結構
  自定義例外繼承 Exception，提供有意義的訊息
  except 從精確到寬泛，最後的 except Exception as e 加 raise
  重要：不要 except Exception: pass（吞掉所有錯誤！）

logging 取代 print
  5 個等級：DEBUG < INFO < WARNING < ERROR < CRITICAL
  TimedRotatingFileHandler → 自動按日期切割日誌檔
  exc_info=True → 附上完整 traceback
  __name__ 作為 logger 名稱 → 自動顯示模組來源

除錯
  breakpoint() → 最快的中斷點設置
  VS Code F9/F5/F10/F11 → 視覺化除錯
  with 語句 → 確保資源（連線/檔案）正確釋放
```

---

*下一篇：[Month05 — Python 與資料庫整合自動化](../05_Month05_Python與資料庫整合自動化/01_SQLAlchemy與psycopg2實務.md)*
